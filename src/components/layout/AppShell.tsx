import { Link, useNavigate, useRouterState } from "@tanstack/react-router";
import { useEffect, useMemo, useState, type ReactNode } from "react";
import { Plus, LayoutDashboard, ArrowLeftRight, Wallet, Settings, RefreshCw, LogOut, Bell, AlertCircle, MoreHorizontal } from "lucide-react";
import { navGroups, quickAddOptions } from "./nav";
import { cn } from "@/lib/utils";
import { useAuth } from "@/hooks/use-auth";
import { useHouseholdTable } from "@/hooks/use-household-data";
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
const bottomItems=[{label:"INÍCIO",to:"/",icon:LayoutDashboard},{label:"MOVIMENTAÇÕES",to:"/movimentacoes",icon:ArrowLeftRight},{label:"AJUSTES",to:"/configuracoes",icon:Settings},{label:"PAINEL",to:"/mais",icon:MoreHorizontal}];
const quickRoutes:Record<string,string>={"NOVA DESPESA":"/despesas","NOVA RECEITA":"/receitas","NOVA PARCELA":"/parcelados","NOVA ASSINATURA":"/assinatura","NOVO INVESTIMENTO":"/investimentos","NOVO RESGATE":"/resgate","NOVA META":"/metas","NOVO CUSTO FIXO":"/custos-fixos"};
function QuickAdd({className}:{className?:string}){const[open,setOpen]=useState(false);const navigate=useNavigate();async function openRegistration(option:string){const destination=quickRoutes[option]??"/";setOpen(false);sessionStorage.setItem("multicap:pending-create",option);await navigate({to:destination as never});const fire=()=>window.dispatchEvent(new CustomEvent("multicap:open-create",{detail:{source:"quick-add",option,title:option}}));window.setTimeout(fire,50);window.setTimeout(fire,250);window.setTimeout(fire,600)}return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><button type="button" aria-label="CADASTRO RÁPIDO" className={cn("gradient-primary shadow-elegant flex h-14 w-14 items-center justify-center rounded-2xl text-primary-foreground transition-all hover:-translate-y-1 hover:shadow-xl active:scale-95",className)}><Plus className="h-7 w-7" strokeWidth={2.5}/></button></DialogTrigger><DialogContent className="max-w-md rounded-3xl"><DialogHeader><DialogTitle className="label-caps text-base">CADASTRO RÁPIDO</DialogTitle></DialogHeader><div className="grid grid-cols-2 gap-2">{quickAddOptions.map(option=><button key={option.label} type="button" onClick={()=>void openRegistration(option.label)} className="label-caps rounded-xl border border-border/70 bg-secondary/60 px-3 py-3 text-center text-[11px] text-foreground transition-all hover:border-primary hover:bg-primary/5 hover:text-primary">{option.label}</button>)}</div></DialogContent></Dialog>}
type TaskNotice={id:string;title:string;due_date:string|null;completed:boolean;household_id:string};function Notifications(){const tasks=useHouseholdTable<TaskNotice>("tasks","id,title,due_date,completed,household_id","due_date");const[open,setOpen]=useState(false);const[todayKey,setTodayKey]=useState("");useEffect(()=>setTodayKey(new Date().toISOString().slice(0,10)),[]);const pending=useMemo(()=>tasks.rows.filter(t=>!t.completed&&t.due_date&&t.due_date<=todayKey),[tasks.rows,todayKey]);return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><button type="button" aria-label="NOTIFICAÇÕES" className="relative rounded-xl border border-border/70 bg-card p-2.5 text-muted-foreground transition-all hover:border-primary/40 hover:text-primary"><Bell className="h-4 w-4"/>{pending.length>0&&<span className="absolute -right-1 -top-1 flex h-4 min-w-4 items-center justify-center rounded-full bg-primary px-1 text-[9px] font-bold text-primary-foreground">{pending.length>9?"9+":pending.length}</span>}</button></DialogTrigger><DialogContent className="max-w-md rounded-3xl"><DialogHeader><DialogTitle className="label-caps">NOTIFICAÇÕES</DialogTitle></DialogHeader><div className="space-y-2">{pending.length===0?<div className="rounded-2xl border border-dashed p-6 text-center"><Bell className="mx-auto mb-2 h-7 w-7 text-muted-foreground"/><p className="text-sm font-semibold">NENHUMA PENDÊNCIA</p></div>:pending.map(task=><div key={task.id} className="flex items-start gap-3 rounded-xl border p-3"><AlertCircle className="mt-0.5 h-4 w-4 shrink-0 text-primary"/><div><p className="text-sm font-semibold">{task.title}</p><p className="text-[10px] text-muted-foreground">{task.due_date===todayKey?"VENCE HOJE":"ATRASADA"}</p></div></div>)}</div></DialogContent></Dialog>}
function SyncIndicator(){return <div className="hidden items-center gap-2 rounded-full border border-success/20 bg-success/5 px-3 py-1.5 text-[10px] text-success lg:flex"><RefreshCw className="h-3.5 w-3.5"/><span className="label-caps">SINCRONIZADO</span></div>};function UserMenu(){const{profile,user,signOut}=useAuth();const displayName=profile?.name??user?.email??"USUÁRIO";const initials=profile?.initials??displayName.slice(0,2).toUpperCase();return <DropdownMenu><DropdownMenuTrigger asChild><button type="button" className="flex items-center gap-3 rounded-2xl border border-transparent px-2 py-1.5 transition-colors hover:border-border hover:bg-card"><span className="flex h-9 w-9 items-center justify-center rounded-full text-[11px] font-bold text-primary-foreground shadow-sm" style={{backgroundColor:profile?.color??"#FF6B00"}}>{initials}</span><div className="hidden leading-tight text-left sm:block"><p className="label-caps text-[11px] font-semibold">{displayName}</p><p className="text-[10px] text-muted-foreground">{user?.email}</p></div></button></DropdownMenuTrigger><DropdownMenuContent align="end" className="rounded-xl"><DropdownMenuItem onClick={()=>void signOut()}><LogOut className="mr-2 h-4 w-4"/>SAIR</DropdownMenuItem></DropdownMenuContent></DropdownMenu>}
export function AppShell({children}:{children:ReactNode}){const pathname=useRouterState({select:s=>s.location.pathname});return <div className="min-h-screen bg-background"><aside className="fixed inset-y-0 left-0 z-30 hidden w-[272px] flex-col border-r border-sidebar-border bg-sidebar/98 shadow-[8px_0_30px_-24px_rgba(0,0,0,.7)] lg:flex"><div className="flex items-center gap-3 border-b border-sidebar-border/70 px-6 py-7"><span className="gradient-primary flex h-11 w-11 items-center justify-center rounded-2xl text-sm font-black text-primary-foreground shadow-lg">M</span><div><span className="label-caps block text-lg font-bold tracking-[0.2em]">MULTICAP</span><span className="text-[9px] tracking-[0.12em] text-muted-foreground">GESTÃO FINANCEIRA</span></div></div><nav className="flex-1 space-y-7 overflow-y-auto px-4 py-6 pb-8">{navGroups.map(group=><div key={group.group}><p className="label-caps px-3 pb-2.5 text-[9px] font-bold tracking-[0.18em] text-muted-foreground/70">{group.group}</p><div className="space-y-1.5">{group.items.map(item=><Link key={item.to} to={item.to as never} className={cn("group label-caps flex items-center gap-3 rounded-2xl border px-3.5 py-3 text-[11px] transition-all duration-200",pathname===item.to?"gradient-soft border-primary/30 font-bold text-primary shadow-sm":"border-transparent text-muted-foreground hover:border-sidebar-border hover:bg-sidebar-accent hover:text-foreground")}><span className={cn("flex h-8 w-8 items-center justify-center rounded-xl transition-colors",pathname===item.to?"bg-primary/10":"bg-secondary/50 group-hover:bg-secondary")}><item.icon className="h-4 w-4"/></span>{item.label}</Link>)}</div></div>)}</nav><div className="border-t border-sidebar-border/70 p-4"><div className="rounded-2xl border border-primary/10 bg-primary/5 p-4"><p className="label-caps text-[9px] font-bold text-primary">CADASTRO RÁPIDO</p><p className="mt-1 text-[10px] leading-relaxed text-muted-foreground">Adicione uma movimentação ou novo item sem sair da sua rotina.</p><div className="mt-3 flex items-center justify-between"><span className="text-[9px] text-muted-foreground">NOVO REGISTRO</span><QuickAdd className="h-10 w-10 rounded-xl"/></div></div></div></aside><div className="lg:pl-[272px]"><div className="sticky top-0 z-20 flex min-h-[72px] items-center justify-between gap-3 border-b border-border/60 bg-background/90 px-5 py-3 backdrop-blur-xl lg:px-10"><div className="hidden lg:block"><p className="label-caps text-[10px] font-semibold text-muted-foreground">MULTICAP</p><p className="text-sm font-semibold">GESTÃO FINANCEIRA</p></div><div className="ml-auto flex items-center gap-3"><SyncIndicator/><Notifications/><UserMenu/></div></div><main className="mx-auto w-full max-w-[1480px] space-y-7 px-5 pb-32 pt-7 lg:px-10 lg:pb-12">{children}</main></div><nav className="fixed inset-x-0 bottom-0 z-40 flex h-[calc(68px+env(safe-area-inset-bottom))] items-end justify-around border-t border-border/70 bg-background/95 px-1 pb-[calc(8px+env(safe-area-inset-bottom))] pt-2 shadow-[0_-12px_35px_-25px_rgba(0,0,0,0.9)] backdrop-blur-xl lg:hidden">{bottomItems.slice(0,2).map(item=><BottomLink key={item.to} item={item} active={pathname===item.to}/>) }<div className="relative -mt-8 flex w-16 justify-center"><QuickAdd className="h-14 w-14 rounded-full shadow-xl ring-4 ring-background"/></div>{bottomItems.slice(2).map(item=><BottomLink key={item.to} item={item} active={pathname===item.to}/>)}</nav></div>};function BottomLink({item,active}:{item:(typeof bottomItems)[number];active:boolean}){return <Link to={item.to as never} className={cn("label-caps flex min-h-12 w-16 flex-col items-center justify-center gap-1 rounded-xl py-1 text-[9px]",active?"bg-primary/10 font-semibold text-primary":"text-muted-foreground")}><item.icon className="h-5 w-5"/>{item.label}</Link>}
