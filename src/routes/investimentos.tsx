import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
import { toast } from "sonner";
import { PageHeader, Panel, ProgressBar, StatCard, Tag } from "@/components/ui-kit";
import { formatCurrency, requiredMonthlyContribution, futureValue, monthsToTarget } from "@/lib/finance";
import { useHouseholdTable } from "@/hooks/use-household-data";
import { MonthSelector, useGlobalMonth } from "@/hooks/use-global-month";
export const Route=createFileRoute("/investimentos")({head:()=>({meta:[{title:"INVESTIMENTOS — MULTICAP"}]}),component:InvestmentsPage});
type Row={id:string;name:string;type:string;invested:number;current_value:number;created_at:string;household_id:string};
function InvestmentsPage(){const{month,setMonth}=useGlobalMonth("investimentos");const{rows,insert,update,remove,isLoading}=useHouseholdTable<Row>("investments","id,name,type,invested,current_value,created_at,household_id");const[name,setName]=useState("");const[type,setType]=useState("RENDA FIXA");const[invested,setInvested]=useState("");const[current,setCurrent]=useState("");const[target,setTarget]=useState(100000);const[initial,setInitial]=useState(0);const[months,setMonths]=useState(60);const[rate,setRate]=useState(10);const[monthly,setMonthly]=useState(1000);const visible=rows.filter(i=>String(i.created_at||"").startsWith(month));const totalInvested=visible.reduce((s,i)=>s+Number(i.invested),0);const totalCurrent=visible.reduce((s,i)=>s+Number(i.current_value),0);const profit=totalCurrent-totalInvested;async function add(){const a=Number(invested.replace(',','.'));const c=current===""?a:Number(current.replace(',','.'));if(!name.trim()||a<=0)return toast.error("PREENCHA NOME E VALOR APLICADO");try{await insert({name:name.trim().toUpperCase(),type,invested:a,current_value:c});setName("");setInvested("");setCurrent("");toast.success("INVESTIMENTO SALVO") }catch(e){toast.error(e instanceof Error?e.message:"Não foi possível salvar")}}async function edit(i:Row){const value=prompt("Valor atual do investimento",String(i.current_value));if(value===null)return;try{await update(i.id,{current_value:Number(value.replace(',','.'))})}catch(e){toast.error(e instanceof Error?e.message:"Não foi possível atualizar")}}async function del(i:Row){if(!confirm(`Excluir ${i.name}?`))return;try{await remove(i.id)}catch(e){toast.error(e instanceof Error?e.message:"Não foi possível excluir")}}const annual=rate/100;return <div className="space-y-5"><PageHeader title="INVESTIMENTOS" subtitle="INVESTIMENTOS REGISTRADOS NO MÊS SELECIONADO." action={<MonthSelector month={month} setMonth={setMonth}/>} /><div className="grid grid-cols-2 gap-3 lg:grid-cols-4"><StatCard label="VALOR APLICADO" value={formatCurrency(totalInvested)} tone="info"/><StatCard label="VALOR ATUAL" value={formatCurrency(totalCurrent)} tone="primary"/><StatCard label="RESULTADO" value={formatCurrency(profit)} tone={profit>=0?"success":"danger"}/><StatCard label="RENTABILIDADE" value={totalInvested?`${((profit/totalInvested)*100).toFixed(1)}%`:"0,0%"} tone="success"/></div><Panel title="NOVO INVESTIMENTO"><div className="grid gap-3 md:grid-cols-5"><label className="md:col-span-2"><span className="label-caps text-[10px] text-muted-foreground">ATIVO / INVESTIMENTO</span><input value={name} onChange={e=>setName(e.target.value)} placeholder="CDB, Tesouro, ETF..." className="mt-1 w-full rounded-xl border border-input bg-background px-3 py-2.5 text-sm"/></label><label><span className="label-caps text-[10px] text-muted-foreground">TIPO</span><select value={type} onChange={e=>setType(e.target.value)} className="mt-1 w-full rounded-xl border border-input bg-background px-3 py-2.5 text-sm"><option>RENDA FIXA</option><option>TESOURO DIRETO</option><option>FII</option><option>ETF</option><option>AÇÕES</option><option>CRIPTO</option><option>OUTROS</option></select></label><label><span className="label-caps text-[10px] text-muted-foreground">VALOR APLICADO</span><input value={invested} onChange={e=>setInvested(e.target.value)} inputMode="decimal" placeholder="0,00" className="mt-1 w-full rounded-xl border border-input bg-background px-3 py-2.5 text-sm"/></label><label><span className="label-caps text-[10px] text-muted-foreground">VALOR ATUAL</span><input value={current} onChange={e=>setCurrent(e.target.value)} inputMode="decimal" placeholder="igual ao aplicado" className="mt-1 w-full rounded-xl border border-input bg-background px-3 py-2.5 text-sm"/></label><button onClick={add} className="gradient-primary label-caps rounded-xl px-4 py-2.5 text-[11px] text-primary-foreground md:col-span-2">SALVAR INVESTIMENTO</button></div></Panel><Panel title={`MINHA CARTEIRA — ${month}`}>{isLoading?<p className="py-8 text-center text-sm text-muted-foreground">Carregando...</p>:visible.length===0?<p className="py-8 text-center text-sm text-muted-foreground">Nenhum investimento cadastrado neste mês.</p>:<div className="space-y-3">{visible.map(i=>{const p=Number(i.current_value)-Number(i.invested);const pct=Number(i.invested)?p/Number(i.invested)*100:0;return <div key={i.id} className="rounded-2xl border border-border bg-card p-4"><div className="flex items-start justify-between gap-3"><div><p className="label-caps text-sm">{i.name}</p><Tag>{i.type}</Tag></div><div className="text-right"><p className="font-bold">{formatCurrency(Number(i.current_value))}</p><p className={pct>=0?"text-xs text-success":"text-xs text-danger"}>{pct>=0?"+":""}{pct.toFixed(1)}%</p></div></div><div className="mt-3"><ProgressBar percent={totalCurrent?Number(i.current_value)/totalCurrent*100:0} tone="primary"/></div><div className="mt-3 flex gap-2"><button onClick={()=>edit(i)} className="label-caps rounded-lg border border-primary/60 px-3 py-1.5 text-[10px] text-primary">ATUALIZAR VALOR</button><button onClick={()=>del(i)} className="label-caps rounded-lg border border-danger/50 px-3 py-1.5 text-[10px] text-danger">EXCLUIR</button></div></div>})}</div>}</Panel><Panel title="CALCULADORA DE APORTES"><div className="grid gap-3 md:grid-cols-5">{[["OBJETIVO",target,setTarget],["VALOR INICIAL",initial,setInitial],["PRAZO (MESES)",months,setMonths],["TAXA A.A. (%)",rate,setRate],["APORTE MENSAL",monthly,setMonthly]].map(([label,value,setter])=><label key={String(label)}><span className="label-caps text-[10px] text-muted-foreground">{String(label)}</span><input type="number" value={value as number} onChange={e=>(setter as (v:number)=>void)(Number(e.target.value))} className="mt-1 w-full rounded-xl border border-input bg-background px-3 py-2.5 text-sm"/></label>)}</div><div className="mt-4 grid gap-3 md:grid-cols-3"><div className="gradient-soft rounded-xl border border-primary/40 p-4"><p className="label-caps text-[10px] text-muted-foreground">APORTE NECESSÁRIO</p><p className="mt-1 text-lg font-bold text-primary">{formatCurrency(requiredMonthlyContribution(target,initial,months,annual))}</p></div><div className="rounded-xl border border-border bg-secondary/40 p-4"><p className="label-caps text-[10px] text-muted-foreground">MONTANTE FINAL</p><p className="mt-1 text-lg font-bold">{formatCurrency(futureValue(monthly,initial,months,annual))}</p></div><div className="rounded-xl border border-border bg-secondary/40 p-4"><p className="label-caps text-[10px] text-muted-foreground">TEMPO NECESSÁRIO</p><p className="mt-1 text-lg font-bold">{monthsToTarget(target,monthly,initial,annual)} meses</p></div></div></Panel></div>}
