import { createFileRoute } from "@tanstack/react-router";
import { useMemo, useState } from "react";
import { toast } from "sonner";
import { PageHeader, Panel, ProgressBar, StatCard, Tag } from "@/components/ui-kit";
import { formatCurrency, calculateBudgetUsage } from "@/lib/finance";
import { useHouseholdTable } from "@/hooks/use-household-data";
import { MonthSelector, useGlobalMonth } from "@/hooks/use-global-month";
import { cn } from "@/lib/utils";
export const Route = createFileRoute("/orcamento")({ head: () => ({ meta: [{ title: "ORÇAMENTO MENSAL — MULTICAP" }] }), component: BudgetPage });
const ALERTS = [50,75,90,100];
type Budget={id:string;category:string;period:string;limit_amount:number;household_id:string}; type Tx={amount:number;type:string;category:string;date:string;household_id:string}; type Category={id:string;category:string;name?:string;kind:string;household_id:string};
function BudgetPage(){const{month:period,setMonth}=useGlobalMonth("orcamento");const[alerts,setAlerts]=useState<number[]>([75,100]);const[newCategory,setNewCategory]=useState("");const[newLimit,setNewLimit]=useState("");const[editingId,setEditingId]=useState<string|null>(null);const[editingName,setEditingName]=useState("");const{rows,insert,update,remove,isLoading}=useHouseholdTable<Budget>("budgets","id,category,period,limit_amount,household_id","category");const{rows:transactions}=useHouseholdTable<Tx>("transactions","amount,type,category,date,household_id","date");const{rows:categoryRows}=useHouseholdTable<Category>("categories","id,name,kind,household_id","name");const budgets=useMemo(()=>rows.filter(b=>b.period===period),[rows,period]);const categories=useMemo(()=>Array.from(new Set(categoryRows.map(c=>(c.name??c.category??"").toUpperCase()).filter(Boolean))),[categoryRows]);const spentByCategory=useMemo(()=>{const map=new Map<string,number>();transactions.filter(t=>t.date.startsWith(period)&&t.type==="DESPESA").forEach(t=>map.set(t.category.toUpperCase(),(map.get(t.category.toUpperCase())??0)+Number(t.amount)));return map},[transactions,period]);const budgetTotal=budgets.reduce((s,b)=>s+Number(b.limit_amount),0);const spentTotal=Array.from(spentByCategory.values()).reduce((s,v)=>s+v,0);async function saveBudget(category:string,value:string){const limit=Number(value.replace(",","."));if(!Number.isFinite(limit)||limit<0)return toast.error("INFORME UM VALOR VÁLIDO");try{const existing=budgets.find(b=>b.category===category);if(existing)await update(existing.id,{limit_amount:limit});else await insert({category,period,limit_amount:limit});toast.success("ORÇAMENTO SALVO")}catch(e){toast.error(e instanceof Error?e.message:"ERRO AO SALVAR")}}async function renameCategory(budget:Budget){const name=editingName.trim().toUpperCase();if(!name)return toast.error("INFORME O NOME DA CATEGORIA");if(name===budget.category)return setEditingId(null);if(budgets.some(b=>b.id!==budget.id&&b.category===name))return toast.error("ESSA CATEGORIA JÁ EXISTE NESTE MÊS");try{await update(budget.id,{category:name});setEditingId(null);toast.success("CATEGORIA ATUALIZADA")}catch(e){toast.error(e instanceof Error?e.message:"ERRO AO EDITAR")}}async function addCategory(){const category=newCategory.trim().toUpperCase();if(!category)return toast.error("INFORME A CATEGORIA");if(!categories.includes(category))return toast.error("CADASTRE ESTA CATEGORIA EM CONFIGURAÇÕES PRIMEIRO");if(budgets.some(b=>b.category===category))return toast.error("ESSA CATEGORIA JÁ ESTÁ NO ORÇAMENTO");await saveBudget(category,newLimit||"0");setNewCategory("");setNewLimit("")}async function deleteBudget(budget:Budget){if(!window.confirm(`EXCLUIR A CATEGORIA ${budget.category} DESTE MÊS?`))return;try{await remove(budget.id);toast.success("CATEGORIA EXCLUÍDA DESTE MÊS")}catch(e){toast.error(e instanceof Error?e.message:"ERRO AO EXCLUIR")}}return <div className="space-y-5"><PageHeader title="ORÇAMENTO MENSAL" subtitle="DEFINA E EDITE O ORÇAMENTO DO MÊS SELECIONADO." action={<MonthSelector month={period} setMonth={setMonth}/>} /><div className="grid grid-cols-2 gap-3 lg:grid-cols-4"><StatCard label="ORÇADO" value={formatCurrency(budgetTotal)} tone="primary"/><StatCard label="GASTO" value={formatCurrency(spentTotal)} tone="danger"/><StatCard label="DISPONÍVEL" value={formatCurrency(budgetTotal-spentTotal)} tone="success"/><StatCard label="UTILIZADO" value={`${budgetTotal?Math.round(calculateBudgetUsage(spentTotal,budgetTotal)):0}%`} tone="info"/></div><Panel title={`CATEGORIAS — ${period}`}>{isLoading?<p className="py-8 text-center text-sm text-muted-foreground">CARREGANDO...</p>:categories.length===0?<p className="py-8 text-center text-sm text-muted-foreground">NENHUMA CATEGORIA CADASTRADA. CADASTRE EM CONFIGURAÇÕES.</p>:<div className="space-y-5">{categories.map(category=>{const budget=budgets.find(b=>b.category===category);const limit=Number(budget?.limit_amount??0);const spent=spentByCategory.get(category)??0;const usage=limit>0?calculateBudgetUsage(spent,limit):0;const over=limit>0&&spent>limit;return <div key={category} className="space-y-2"><div className="flex flex-wrap items-center justify-between gap-2">{editingId===budget?.id?<div className="flex gap-2"><input autoFocus value={editingName} onChange={e=>setEditingName(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&budget)void renameCategory(budget);if(e.key==="Escape")setEditingId(null)}} className="rounded-lg border border-primary bg-background px-2.5 py-1.5 text-xs"/><button onClick={()=>budget&&void renameCategory(budget)} className="label-caps rounded-lg bg-primary px-3 py-1.5 text-[10px] text-primary-foreground">SALVAR</button><button onClick={()=>setEditingId(null)} className="label-caps rounded-lg border px-3 py-1.5 text-[10px]">CANCELAR</button></div>:<span className="label-caps text-[11px]">{category}</span>}<span className="text-sm font-semibold">{formatCurrency(spent)} <span className="text-muted-foreground">/ {formatCurrency(limit)}</span></span></div><ProgressBar percent={usage}/><div className="flex flex-wrap items-center gap-2">{budget?<><input defaultValue={limit?String(limit).replace(".",","):""} placeholder="DEFINIR LIMITE" inputMode="decimal" className="w-36 rounded-lg border border-input bg-background px-2.5 py-1.5 text-xs" id={`budget-${budget.id}`}/><button onClick={()=>saveBudget(category,(document.getElementById(`budget-${budget.id}`) as HTMLInputElement)?.value??"0")} className="label-caps rounded-lg border border-primary/60 px-3 py-1.5 text-[10px] text-primary">SALVAR LIMITE</button><button onClick={()=>{setEditingId(budget.id);setEditingName(budget.category)}} className="label-caps rounded-lg border border-info/60 px-3 py-1.5 text-[10px] text-info">EDITAR NOME</button><button onClick={()=>void deleteBudget(budget)} className="label-caps rounded-lg border border-danger/50 px-3 py-1.5 text-[10px] text-danger">EXCLUIR</button></>:<button onClick={()=>{setNewCategory(category);setNewLimit("")}} className="label-caps rounded-lg border border-primary/60 px-3 py-1.5 text-[10px] text-primary">ADICIONAR AO ORÇAMENTO</button>}<span className={cn("text-[11px]",over?"text-danger":"text-muted-foreground")}>{limit?`${Math.round(usage)}% UTILIZADO`:"SEM LIMITE DEFINIDO"}</span></div></div>})}</div>}</Panel><Panel title="ADICIONAR CATEGORIA AO ORÇAMENTO"><div className="flex flex-wrap gap-2"><select value={newCategory} onChange={e=>setNewCategory(e.target.value)} className="min-w-48 rounded-xl border border-input bg-background px-3 py-2.5 text-sm"><option value="">SELECIONE UMA CATEGORIA</option>{categories.filter(c=>!budgets.some(b=>b.category===c)).map(c=><option key={c}>{c}</option>)}</select><input value={newLimit} onChange={e=>setNewLimit(e.target.value)} placeholder="LIMITE MENSAL" inputMode="decimal" className="w-40 rounded-xl border border-input bg-background px-3 py-2.5 text-sm"/><button onClick={()=>void addCategory()} className="gradient-primary rounded-xl px-4 py-2.5 text-[11px] text-primary-foreground">ADICIONAR</button></div></Panel><Panel title="ALERTAS CONFIGURÁVEIS"><div className="flex flex-wrap gap-2">{ALERTS.map(a=>{const active=alerts.includes(a);return <button key={a} onClick={()=>setAlerts(p=>active?p.filter(x=>x!==a):[...p,a])} className={cn("label-caps rounded-xl border px-4 py-2 text-[11px]",active?"border-primary bg-primary/15 text-primary":"border-border text-muted-foreground")}>{a}%</button>})}</div><div className="mt-3 flex gap-2"><Tag tone="warning">PRÓXIMO DO LIMITE</Tag><Tag tone="danger">ACIMA DO ORÇAMENTO</Tag></div></Panel></div>}
