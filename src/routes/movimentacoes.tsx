import { createFileRoute } from "@tanstack/react-router";
import { CreditCard, Search, CalendarDays, ArrowLeftRight } from "lucide-react";
import { useMemo, useState } from "react";
import { PageHeader, Panel, StatCard, Tag, PersonDot } from "@/components/ui-kit";
import { formatCurrency } from "@/lib/finance";
import { cn } from "@/lib/utils";
import { useHouseholdTable } from "@/hooks/use-household-data";
import { MonthSelector, useGlobalMonth } from "@/hooks/use-global-month";
export const Route=createFileRoute("/movimentacoes")({head:()=>({meta:[{title:"MOVIMENTAÇÕES — MULTICAP"},{name:"description",content:"Extrato de cartão de crédito, alimentação e refeições."}]}),component:MovimentacoesPage});
type TxType="RECEITA"|"DESPESA"|"TRANSFERENCIA"|"INVESTIMENTO";type Row={id:string;date:string;description:string;category:string;pay_method:string;responsible:string;amount:number;type:TxType;paid:boolean;household_id:string};
const isCard=(t:Row)=>/cart[aã]o|cr[eé]dito/.test(`${t.description} ${t.category} ${t.pay_method}`.toLowerCase());
const isFood=(t:Row)=>/alimenta[cç][aã]o|mercado|supermercado|comida|refei[cç][aã]o|restaurante|lanch|delivery/.test(`${t.description} ${t.category}`.toLowerCase());
function MovimentacoesPage(){const{month,setMonth}=useGlobalMonth("movimentacoes");const tx=useHouseholdTable<Row>("transactions","id,date,description,category,pay_method,responsible,amount,type,paid,household_id");const[search,setSearch]=useState("");const[filter,setFilter]=useState<"TODOS"|"CARTAO"|"ALIMENTACAO"|"REFEICAO">("TODOS");const visible=useMemo(()=>tx.rows.filter(t=>t.date.startsWith(month)).filter(t=>filter==="TODOS"?isCard(t)||isFood(t):filter==="CARTAO"?isCard(t):filter==="ALIMENTACAO"?/alimenta[cç][aã]o|mercado|supermercado|comida/.test(`${t.description} ${t.category}`.toLowerCase()):/refei[cç][aã]o|restaurante|lanch|delivery/.test(`${t.description} ${t.category}`.toLowerCase())).filter(t=>`${t.description} ${t.category} ${t.pay_method} ${t.responsible}`.toLowerCase().includes(search.toLowerCase())).sort((a,b)=>b.date.localeCompare(a.date)),[tx.rows,month,filter,search]);const card=visible.filter(isCard).reduce((s,t)=>s+Number(t.amount),0);const food=visible.filter(isFood).reduce((s,t)=>s+Number(t.amount),0);const total=visible.filter(t=>t.type==="DESPESA").reduce((s,t)=>s+Number(t.amount),0);return <div className="space-y-5"><PageHeader title="MOVIMENTAÇÕES" subtitle="EXTRATO DE CARTÃO DE CRÉDITO, ALIMENTAÇÃO E REFEIÇÕES." action={<MonthSelector month={month} setMonth={setMonth}/>}/><div className="grid grid-cols-2 gap-3 lg:grid-cols-3"><StatCard label="TOTAL" value={formatCurrency(total)} tone="danger"/><StatCard label="CARTÃO DE CRÉDITO" value={formatCurrency(card)} tone="primary"/><StatCard label="ALIMENTAÇÃO / REFEIÇÕES" value={formatCurrency(food)} tone="warning"/></div><Panel><div className="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><div className="relative min-w-0 flex-1 lg:max-w-md"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="BUSCAR NO EXTRATO..." className="w-full rounded-xl border border-input bg-background py-2.5 pl-9 pr-3 text-sm outline-none focus:border-primary"/></div><div className="flex flex-wrap gap-2">{(["TODOS","CARTAO","ALIMENTACAO","REFEICAO"] as const).map(f=><button key={f} type="button" onClick={()=>setFilter(f)} className={cn("label-caps rounded-lg border px-3 py-2 text-[10px]",filter===f?"border-primary bg-primary/10 text-primary":"border-border text-muted-foreground")}>{f==="CARTAO"?"CARTÃO DE CRÉDITO":f==="ALIMENTACAO"?"ALIMENTAÇÃO":f==="REFEICAO"?"REFEIÇÕES":"TODOS"}</button>)}</div></div></Panel><Panel title={`EXTRATO — ${month}`}>{tx.isLoading?<p className="py-10 text-center text-sm text-muted-foreground">CARREGANDO EXTRATO...</p>:visible.length===0?<div className="py-12 text-center"><CreditCard className="mx-auto mb-3 h-8 w-8 text-muted-foreground"/><p className="text-sm font-semibold">NENHUMA MOVIMENTAÇÃO DESTE TIPO</p><p className="mt-1 text-xs text-muted-foreground">Cartão de crédito, alimentação e refeições aparecem aqui. O restante fica no Fluxo.</p></div>:<div className="divide-y divide-border">{visible.map(t=><div key={t.id} className="flex items-center gap-3 py-3.5"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-primary/10 text-primary"><CreditCard className="h-5 w-5"/></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{t.description}</p><div className="mt-1 flex flex-wrap items-center gap-2"><Tag>{t.category}</Tag><PersonDot name={t.responsible}/><span className="flex items-center gap-1 text-[10px] text-muted-foreground"><CalendarDays className="h-3 w-3"/>{new Date(`${t.date}T12:00:00`).toLocaleDateString("pt-BR")}</span><span className="text-[10px] text-muted-foreground">{t.pay_method}</span></div></div><div className="text-right"><p className={cn("text-sm font-bold",t.type==="RECEITA"?"text-success":"text-danger")}>{t.type==="RECEITA"?"+":"-"}{formatCurrency(Math.abs(Number(t.amount)))}</p><Tag tone={t.paid?"success":"warning"}>{t.paid?"PAGO":"PENDENTE"}</Tag></div></div>)}</div>}</Panel></div>}
