import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useMemo, useState } from "react";
import { PageHeader, Panel } from "@/components/ui-kit";
import { formatCurrency } from "@/lib/finance";
import { useAuth } from "@/hooks/use-auth";
import { supabase } from "@/integrations/supabase/client";
import { cn } from "@/lib/utils";
export const Route=createFileRoute("/calendario")({head:()=>({meta:[{title:"CALENDÁRIO — HARMONY HUB"}]}),component:CalendarPage});
type Tx={id:string;date:string;description:string;amount:number;category:string;responsible:string;type:string;paid:boolean;household_id:string}; type Payable={id:string;due_date:string;amount:number;status:string;household_id:string}; type ColorFilter="spend"|"income"|"payable";
const WEEK=["DOM","SEG","TER","QUA","QUI","SEX","SÁB"]; const START_MONTH="2026-07"; const END_MONTH="2027-12";
function currentMonth(){const now=new Date();const value=`${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,"0")}`;return value<START_MONTH?START_MONTH:value>END_MONTH?END_MONTH:value}
function CalendarPage(){const{profile}=useAuth();const[month,setMonthState]=useState(currentMonth);const[selected,setSelected]=useState<number|null>(null);const[colorFilters,setColorFilters]=useState<Set<ColorFilter>>(new Set());const[tx,setTx]=useState<Tx[]>([]);const[payables,setPayables]=useState<Payable[]>([]);
useEffect(()=>{try{const saved=localStorage.getItem("harmony-month-calendario");if(saved&&saved>=START_MONTH&&saved<=END_MONTH)setMonthState(saved)}catch{}},[]);
useEffect(()=>{let cancelled=false;const householdId=profile?.household_id;if(!householdId){setTx([]);setPayables([]);return()=>{cancelled=true}};const load=async()=>{const[transactionsResult,payableResult]=await Promise.all([supabase.from("transactions").select("id,date,description,amount,category,responsible,type,paid,household_id").eq("household_id",householdId).order("date",{ascending:false}),supabase.from("accounts_payable").select("id,due_date,amount,status,household_id").eq("household_id",householdId).order("due_date",{ascending:false})]);if(cancelled)return;if(!transactionsResult.error)setTx((transactionsResult.data??[]) as Tx[]);if(!payableResult.error)setPayables((payableResult.data??[]) as Payable[])};void load();return()=>{cancelled=true}},[profile?.household_id]);
const setMonth=(next:string)=>{if(next<START_MONTH||next>END_MONTH)return;setMonthState(next);try{localStorage.setItem("harmony-month-calendario",next)}catch{}};
const[year,monthNumber]=month.split("-").map(Number) as [number,number];const days=new Date(year,monthNumber,0).getDate();const first=new Date(year,monthNumber-1,1).getDay();const monthly=useMemo(()=>tx.filter(t=>t.date.startsWith(month)),[tx,month]);const monthlyPayables=useMemo(()=>payables.filter(p=>p.due_date.startsWith(month)&&p.status==="PENDENTE"),[payables,month]);const now=new Date();const todayKey=`${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,"0")}-${String(now.getDate()).padStart(2,"0")}`;const spendByDay=useMemo(()=>{const m=new Map<number,number>();monthly.filter(t=>t.type==="DESPESA").forEach(t=>{const d=Number(t.date.slice(-2));m.set(d,(m.get(d)??0)+Number(t.amount))});return m},[monthly]);const incomeByDay=useMemo(()=>{const m=new Map<number,number>();monthly.filter(t=>t.type==="RECEITA").forEach(t=>{const d=Number(t.date.slice(-2));m.set(d,(m.get(d)??0)+Number(t.amount))});return m},[monthly]);const payableByDay=useMemo(()=>{const m=new Map<number,number>();monthlyPayables.forEach(p=>{const d=Number(p.due_date.slice(-2));m.set(d,(m.get(d)??0)+Number(p.amount))});return m},[monthlyPayables]);const selectedDay=Math.min(selected??(month===todayKey.slice(0,7)?now.getDate():1),days);const selectedKey=`${month}-${String(selectedDay).padStart(2,"0")}`;const selectedTx=monthly.filter(t=>t.date===selectedKey);const selectedSpend=selectedTx.filter(t=>t.type==="DESPESA").reduce((s,t)=>s+Number(t.amount),0);const selectedIncome=selectedTx.filter(t=>t.type==="RECEITA").reduce((s,t)=>s+Number(t.amount),0);const selectedPayable=monthlyPayables.filter(p=>p.due_date===selectedKey).reduce((s,p)=>s+Number(p.amount),0);
function toggleColorFilter(filter:ColorFilter){setColorFilters(current=>{const next=new Set(current);if(next.has(filter))next.delete(filter);else next.add(filter);return next})}
return <div className="space-y-5"><PageHeader title="CALENDÁRIO" subtitle="VISÃO FINANCEIRA DIÁRIA: GASTOS, RECEBIMENTOS E CONTAS A PAGAR." action={<select aria-label="SELECIONAR MÊS" value={month} onChange={e=>setMonth(e.target.value)} className="rounded-xl border border-input bg-background px-3 py-2 text-xs font-semibold uppercase">{Array.from({length:18},(_,i)=>{const d=new Date(2026,6+i,1);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}`}).map(v=><option key={v} value={v}>{new Date(Number(v.slice(0,4)),Number(v.slice(5,7))-1,1).toLocaleDateString("pt-BR",{month:"long",year:"numeric"]).toUpperCase()}</option>)}</select>}/>}><Panel><div className="mb-3 flex flex-wrap gap-2 text-[9px] font-semibold uppercase tracking-wide">{(["spend","income","payable"] as ColorFilter[]).map(filter=>{const labels={spend:"● GASTOS",income:"● RECEBIMENTOS",payable:"● A PAGAR"};const text={spend:"text-danger",income:"text-success",payable:"text-warning"};return <button key={filter} type="button" aria-pressed={colorFilters.has(filter)} onClick={()=>toggleColorFilter(filter)} className={cn("rounded-full border px-3 py-1.5 transition-all",colorFilters.has(filter)?"border-primary bg-primary/10":"border-border",text[filter])}>{labels[filter]}</button>})}</div><p className="mb-3 text-[9px] uppercase tracking-wide text-muted-foreground">{colorFilters.size===0?"MOSTRANDO TODAS AS CORES":"MOSTRANDO APENAS AS CORES SELECIONADAS"}</p><div className="mb-2 grid grid-cols-7 gap-1.5">{WEEK.map(d=><span key={d} className="label-caps text-center text-[10px] text-muted-foreground">{d}</span>)}</div><div className="grid grid-cols-7 gap-1.5">{Array.from({length:first}).map((_,i)=><span key={`empty-${i}`}/>)}{Array.from({length:days},(_,i)=>i+1).map(day=>{const dayKey=`${month}-${String(day).padStart(2,"0")}`;const spend=spendByDay.get(day)??0;const income=incomeByDay.get(day)??0;const payable=payableByDay.get(day)??0;const isToday=dayKey===todayKey;const isSelected=selectedDay===day;const hasFiltered=(colorFilters.has("spend")&&spend>0)||(colorFilters.has("income")&&income>0)||(colorFilters.has("payable")&&payable>0);return <button type="button" key={day} onClick={()=>setSelected(day)} className={cn("relative flex min-h-[92px] flex-col items-center justify-start gap-1 rounded-xl border p-2 text-[11px] transition-all hover:border-primary hover:bg-primary/5",isToday?"border-primary ring-2 ring-primary":"border-border/60",isSelected&&!isToday&&"ring-2 ring-primary",colorFilters.size>0&&!hasFiltered&&"opacity-25")}><span className="font-bold">{day}</span>{(colorFilters.size===0||colorFilters.has("spend"))&&spend>0&&<span className="max-w-full truncate text-[8px] font-semibold text-danger">− {formatCurrency(spend)}</span>}{(colorFilters.size===0||colorFilters.has("income"))&&income>0&&<span className="max-w-full truncate text-[8px] font-semibold text-success">+ {formatCurrency(income)}</span>}{(colorFilters.size===0||colorFilters.has("payable"))&&payable>0&&<span className="max-w-full truncate text-[8px] font-semibold text-warning">A PAGAR {formatCurrency(payable)}</span>}</button>})}</div></Panel><Panel title={`${selectedKey} — RESUMO FINANCEIRO`}><div className="grid grid-cols-1 gap-3 sm:grid-cols-3"><div className="rounded-xl border border-border bg-secondary/40 p-3"><p className="label-caps text-[10px] text-muted-foreground">GASTOS</p><p className="mt-1 text-sm font-bold text-danger">{formatCurrency(selectedSpend)}</p></div><div className="rounded-xl border border-border bg-secondary/40 p-3"><p className="label-caps text-[10px] text-muted-foreground">RECEBIMENTOS</p><p className="mt-1 text-sm font-bold text-success">{formatCurrency(selectedIncome)}</p></div><div className="rounded-xl border border-border bg-secondary/40 p-3"><p className="label-caps text-[10px] text-muted-foreground">A PAGAR</p><p className="mt-1 text-sm font-bold text-warning">{formatCurrency(selectedPayable)}</p></div></div><p className="mt-4 text-center text-[10px] text-muted-foreground">SELECIONE OUTRO DIA NO CALENDÁRIO PARA CONSULTAR O RESUMO.</p></Panel></div></div>}
