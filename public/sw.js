self.addEventListener('push',event=>{let data={title:'MULTICAP',body:'Você tem uma nova notificação.',data:{}};try{data=event.data?.json()||data}catch{}event.waitUntil(self.registration.showNotification(data.title||'MULTICAP',{body:data.body||'',icon:'/favicon.ico',badge:'/favicon.ico',data:data.data||{}}))});self.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus' in c)return c.focus()}return clients.openWindow('/')}))});